# URL de l'API SafeAlert (sans slash final)
# En dev avec proxy Vite, laisser vide ou utiliser /api relatif
VITE_API_BASE_URL=http://localhost:3000/api

# Web OAuth client ID (public, même valeur que GOOGLE_CLIENT_ID backend)
# VITE_GOOGLE_CLIENT_ID=xxxx.apps.googleusercontent.com
